teleop:
  type: so101_leader
  port: COM3
  id: 2026-08-14_leader
  calibration_dir: ./calibration

robot:
  type: so101_follower
  port: COM4
  id: 2026-08-14_follower
  calibration_dir: ./calibration
  cameras:
    wrist:
      type: opencv
      index_or_path: 0
      width: 640
      height: 480
      fps: 30
    front:
      type: opencv
      index_or_path: 1
      width: 640
      height: 480
      fps: 30

dataset:
  repo_id: <HF_USER>/so101_pick_place
  num_episodes: 20
  single_task: "Pick up the object and place it into the box"
  streaming_encoding: true

display_data: true
