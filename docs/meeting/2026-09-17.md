# 2026-09-17（四）現場進度整理（延續到 9/18 凌晨）

**類型：現場工作紀錄，不是會議。** 操作者：柏宇（git user `Boyu`）。
來源標記同 `CLAUDE.md` §6-b。本份只放摘要與指標，不另立第二個真相來源。

---

## 1. 閉環評估錄影：資料夾 ↔ 模型對照

錄影設定：`configs/rollout_omx_b1_uvc60_eval.yaml`（ACT）／`configs/rollout_omx_b1_diffusion_eval.yaml`（Diffusion），
`strategy: episodic`，每集 20 s、reset 120 s，**未上傳 Hub**（只在筆電 `D:\hf\lerobot\ericc430\`）。
每次執行實際用的模型以 `config_records/2026-09-17_omx_follower/` 的 `cli overrides` 行為準。

| 資料夾（`D:\hf\lerobot\ericc430\`） | 模型 | 集數 | 用途 | 來源 |
|---|---|---|---|---|
| `rollout_omx_b1_uvc60_paper_cup_20260917_234916` | `ericc430/act_omx_b1_uvc60`（20k） | 30 | 閉環評估 | `[產出物]` `meta/info.json` |
| `rollout_omx_b1_uvc60_40k_paper_cup_20260918_005629` | `ericc430/act_omx_b1_uvc60_40k` | 5（中止） | **未跑完、放棄**：`[柏宇說]` 2026-09-18「覺得跟20k的差不多」（主觀觀察，5 集未逐集判讀） | `[產出物]` `config_records/…__6be0f432.yaml` |
| **`rollout_omx_b1_uvc60_100k_paper_cup_20260918_010912`** | **`ericc430/act_omx_b1_uvc60_100k`** | **36**（10552 幀，每集 287–296，兩台相機同步） | **100k 的 close loop 測試集**，已跑完 | `[柏宇說]` 2026-09-18「010902是那個100k模型的close loop測試集」；實際資料夾名是 `010912`（只有這一個 100k 資料夾，`config_records/…__2c355eb4.yaml` 01:08:59 的 `cli overrides` = 100k） |
| `rollout_omx_b1_diffusion_paper_cup_20260918_004502` | `ericc430/diffusion_omx_b1_uvc60` | 1 | 測試（待刪） | `[產出物]` |

**100k close loop 的外部錄影（2026-09-19 補記）：**這批影片已經傳到這台電腦。
- 出處：`[柏宇說]` 2026-09-19「我把紙杯的close loop傳進來了」「先記錄我有傳上去了」。
- 位置：`.cache/lerobot/omx_pick_place_pilot_uvc_100k_paper_cup_close_loop36_0918/` `[產出物]`。
- 內容：36 支 mp4，共 1.5 GB，1920×1080、60 fps、有聲音，不是 LeRobot 格式（沒有 `meta/`）。
  檔名時間從 `20260918_010913` 到 `014045`，跟上表的 `010912` 那一次是同一段時間。
  **影片依時間戳記由小到大，對應 ep 0–35**：`[柏宇說]` 2026-09-19「根據時間戳從小到大，我是照順序拍的」。
  2026-09-19 已經把檔名改成 `epNN_<原始時間戳>.mp4`（例如 `ep00_20260918_010913.mp4`），對照表放在同一個資料夾的 `episode_video_map.csv`，裡面保留了原始檔名。
- **原檔是手機錄的 HEVC Main 10、HLG HDR（BT.2020），Windows 沒裝 HEVC 擴充就開不起來。**
  2026-09-19 已經轉成 `h264/epNN_*.mp4`：36 支，共 292 MB，H.264、yuv420p、BT.709 SDR，1080p60，聲音照原檔保留。
  轉檔時先把 HDR tonemap 成 SDR，用的是 ffmpeg 的 `zscale` 加 `tonemap=hable`。
  檢查過每支的長度跟原檔相差都不到 0.1 s；ep00 抽了一張畫面目視，顏色正常 `[已查證]`。
  **看影片或標註時請用 `h264/` 裡的檔案**；原始的 HEVC 檔還留著，沒有刪。
- ⚠️ 每支影片長 8–26 s，是手動開始、停止錄影的，影片裡的時間**不能直接換算成 episode 的幀數**。
- ⚠️ 這份資料**不在 Hub 上**：2026-09-19 查過 `ericc430` 帳號底下的 datasets，沒有找到。另外 `.cache` 被 gitignore 了，所以這 1.5 GB 目前只存在這台電腦上。

**20k close loop 的外部錄影（2026-09-19 補記）：**
- 位置：`.cache/lerobot/omx_pick_place_pilot_uvc_20k_paper_cup_close_loop29_0918/` `[產出物]`。
- 內容：29 支，檔名時間從 `20260917_235032` 到 `20260918_002127`，格式跟 100k 那批一樣，都是 HEVC HLG HDR。
- 已轉成 `h264/`（原始檔名不變）：29 支，共 263 MB，每支都是 h264，長度跟原檔相差都不到 0.1 s `[已查證]`。
- 🟡 **還沒對應到 episode：影片 29 支，rollout `..._234916` 卻有 30 集。**
  rollout 從 23:49:16 開始，第一支影片是 23:50:32，晚了 76 s。影片之間平常的間隔是 43–70 s，
  所以看起來像是 ep 0 沒拍到 `[AI推論]`，要等柏宇確認之後才改名。

已刪除的測試錄影（不要再找）：20k 的 `232802`、`233132`、`234530`；diffusion 的 `004000`；100k 的 `010334`（3 集）。

⚠️ **20k 是 30 集、100k 是 36 集**（`num_episodes` 在兩次之間由 30 改成 36，`config_records/…__2c355eb4.yaml`）。
比較成功率要用比例，或只比兩邊相同擺放位置的集；哪 36 個位置 `[未確認]`。

## 2. 待補

- 各資料夾逐集成功／失敗 → eval CSV（`eval/README.md` 格式）。
- Diffusion 在 RTX 3050 上 100 步 DDPM 每次推論卡 2.5–3.3 s（20 s 只存 129 幀）→ `[AI提議]` 🟡 改 DDIM 10 步，待柏宇決定。
